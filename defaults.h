#ifndef DEFAULTS_H
#define DEFAULTS_H

/* Default dir inside user's home directory.  Specifies
    connections to make and actions to perform. */
#define DEFAULT_CONF_DIR ".netbiff"

/* Default number of seconds between each mail check. */
#define DEFAULT_POLL_FREQUENCY 60

/* The shell with which to execute actions/commands. */
#define SHELL "/bin/sh"

#endif /* DEFAULTS_H */
